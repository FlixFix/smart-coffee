/**
 * Enum used to define the brew time for the coffee.
 */
export enum BrewTypeEnum {
    /**
     * Single shot.
     */
    SINGLE = 'single',
    /**
     * Double shot.
     */
    DOUBLE = 'double'
}